# Чеклист сдачи — глава 21 (финальный проект)

Пройди все **6 пунктов** перед отправкой `library_project` на проверку.

---

## 1. Структура проекта

- [ ] Есть файлы: `app.py`, `database.py`, `requirements.txt`, `.gitignore`, `tests/test_api.py`, `README.md`
- [ ] Папка `venv/` и `library.db` **не** закоммичены в Git (есть в `.gitignore`)

---

## 2. База данных

- [ ] `init_db()` создаёт таблицы `books`, `readers`, `loans` с FOREIGN KEY
- [ ] Все INSERT используют плейсхолдеры `?`, не f-string в SQL
- [ ] После `python -c "import database as db; db.init_db()"` появляется рабочая БД без ошибок

---

## 3. Flask API

- [ ] Работают эндпоинты: `GET/POST /api/books`, `GET /api/books/<id>`, `GET/POST /api/readers`, `GET/POST /api/loans`
- [ ] POST без обязательных полей возвращает **400** с `{"error": "..."}`
- [ ] Несуществующий id книги возвращает **404**
- [ ] Успешное создание книги / читателя / выдачи — **201**

---

## 4. Тесты pytest

- [ ] Минимум **5** тестов в `tests/test_api.py`
- [ ] Фикстура `client` подменяет `DB_PATH` через `tmp_path` и `monkeypatch`
- [ ] Команда `pytest tests/ -v` завершается с **всеми passed** (зелёные)

---

## 5. README

- [ ] Описана установка зависимостей (`pip install -r requirements.txt`)
- [ ] Описан запуск (`python app.py`)
- [ ] Описан запуск тестов (`pytest tests/ -v`)
- [ ] Указаны основные URL API (хотя бы таблицей)

---

## 6. Git (или архив)

- [ ] Минимум **3** осмысленных коммита (например: database → routes → tests)
- [ ] **Или** zip-архив `library_project` с полным кодом и README
- [ ] При сдаче на GitHub — ссылка на репозиторий открывается и клонируется

---

## Бонус (не обязательно)

- [ ] Реализовано одно дополнение: DELETE книги, поиск `?q=`, или проверка «книга уже выдана»
- [ ] В README кратко описано, что добавлено

---

**Готово к сдаче?** Отметь все 6 обязательных блоков и приложи ссылку на GitHub или zip + скриншот `pytest tests/ -v`.